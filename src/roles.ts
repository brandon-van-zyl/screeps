export type Roles = "harvester" | "builder" | "upgrader" | "defender" | "claimer" | "miner";

export const roles = {
  harvester: "harvester",
  builder: "builder",
  upgrader: "upgrader",
  defender: "defender",
  claimer: "claimer",
  miner: "miner"
};
